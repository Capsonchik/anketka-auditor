# Скопируй в .env.local для локальной разработки.
# На проде не задавай API_URL — останется https://survey-all.ru

# Origin локального FastAPI (tunnel compose / uvicorn на :8000)
API_URL=http://127.0.0.1:8000
